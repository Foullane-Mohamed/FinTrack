<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Savings Goals - FinTrack</title>
    <link href="/css/output.css" rel="stylesheet" />
  </head>
  <body class="bg-gray-100 min-h-screen font-sans">
    <nav class="bg-blue-600 text-white p-4">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-xl font-bold">FinTrack</h1>
        <div class="flex items-center space-x-4">
          <span>Hello, <%= user.username %></span>
          <a
            href="/auth/logout"
            class="bg-red-500 hover:bg-red-700 px-3 py-1 rounded"
            >Logout</a
          >
        </div>
      </div>
    </nav>

    <div class="container mx-auto mt-8 px-4">
      <div class="max-w-6xl mx-auto bg-white rounded-lg shadow-md p-6">
        <h1 class="text-3xl font-bold text-gray-800 mb-6">Savings Goals</h1>

        <div class="mb-6 pb-4 border-b border-gray-200">
          <nav class="flex space-x-4">
            <a
              href="/auth/dashboard"
              class="text-blue-600 hover:text-blue-800 px-3 py-1 rounded hover:bg-blue-50"
              >Dashboard</a
            >
            <a
              href="/transactions"
              class="text-blue-600 hover:text-blue-800 px-3 py-1 rounded hover:bg-blue-50"
              >Transactions</a
            >
            <a
              href="/budgets"
              class="text-blue-600 hover:text-blue-800 px-3 py-1 rounded hover:bg-blue-50"
              >Budgets</a
            >
            <a
              href="/auth/logout"
              class="text-blue-600 hover:text-blue-800 px-3 py-1 rounded hover:bg-blue-50"
              >Logout</a
            >
          </nav>
        </div>

        <div class="mb-8 bg-gray-50 p-6 rounded-lg">
          <h2 class="text-xl font-semibold text-gray-800 mb-4">Add New Goal</h2>
          <form
            action="/goals/create"
            method="POST"
            class="grid grid-cols-1 md:grid-cols-4 gap-4"
          >
            <input
              type="text"
              name="title"
              placeholder="Goal title"
              required
              class="px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            />
            <input
              type="number"
              step="0.01"
              name="targetAmount"
              placeholder="Target amount"
              required
              class="px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            />
            <input
              type="date"
              name="deadline"
              class="px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            />
            <button
              type="submit"
              class="bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-4 rounded-lg transition-colors"
            >
              Add Goal
            </button>
          </form>
        </div>

        <% if (goals.length > 0) { %>
        <div class="overflow-x-auto">
          <table class="min-w-full bg-white border border-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider border-b"
                >
                  Title
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider border-b"
                >
                  Target Amount
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider border-b"
                >
                  Current Amount
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider border-b"
                >
                  Deadline
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider border-b"
                >
                  Progress
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider border-b"
                >
                  Actions
                </th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% goals.forEach(goal => { %>
              <tr class="hover:bg-gray-50">
                <td
                  class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900"
                >
                  <%= goal.title %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  $<%= goal.targetAmount %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  $<%= goal.currentAmount || 0 %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <%= goal.deadline ? new Date(goal.deadline).toDateString() :
                  '-' %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <div class="w-full bg-gray-200 rounded-full h-2.5">
                    <div
                      class="bg-blue-600 h-2.5 rounded-full"
                      style="
                        width: <%=
                          Math.min(
                            100,
                            ((goal.currentAmount || 0) / goal.targetAmount) *
                              100
                          )
                          %>%;
                      "
                    ></div>
                  </div>
                  <span class="text-xs text-gray-600 mt-1 block">
                    <%= Math.round(((goal.currentAmount || 0) /
                    goal.targetAmount) * 100) %>%
                  </span>
                </td>
                <td
                  class="px-6 py-4 whitespace-nowrap text-sm font-medium space-x-2"
                >
                  <button
                    onclick="toggleEditForm('<%= goal.id %>')"
                    class="bg-yellow-500 hover:bg-yellow-600 text-white text-xs px-3 py-1 rounded"
                  >
                    Edit
                  </button>
                  <form
                    action="/goals/delete/<%= goal.id %>"
                    method="POST"
                    class="inline"
                  >
                    <button
                      type="submit"
                      class="bg-red-500 hover:bg-red-600 text-white text-xs px-3 py-1 rounded"
                      onclick="return confirm('Are you sure you want to delete this goal?')"
                    >
                      Delete
                    </button>
                  </form>

                  <div
                    id="editForm-<%= goal.id %>"
                    class="hidden mt-4 p-4 bg-gray-50 rounded border"
                  >
                    <form
                      action="/goals/update/<%= goal.id %>"
                      method="POST"
                      class="grid grid-cols-1 md:grid-cols-5 gap-2"
                    >
                      <input
                        type="text"
                        name="title"
                        value="<%= goal.title %>"
                        required
                        class="px-2 py-1 border border-gray-300 rounded text-xs"
                        placeholder="Title"
                      />
                      <input
                        type="number"
                        step="0.01"
                        name="targetAmount"
                        value="<%= goal.targetAmount %>"
                        required
                        class="px-2 py-1 border border-gray-300 rounded text-xs"
                        placeholder="Target Amount"
                      />
                      <input
                        type="number"
                        step="0.01"
                        name="currentAmount"
                        value="<%= goal.currentAmount || 0 %>"
                        required
                        class="px-2 py-1 border border-gray-300 rounded text-xs"
                        placeholder="Current Amount"
                      />
                      <input
                        type="date"
                        name="deadline"
                        value="<%= goal.deadline ? goal.deadline.toISOString().split('T')[0] : '' %>"
                        class="px-2 py-1 border border-gray-300 rounded text-xs"
                      />
                      <div class="flex space-x-1">
                        <button
                          type="submit"
                          class="bg-blue-500 hover:bg-blue-600 text-white text-xs px-2 py-1 rounded"
                        >
                          Update
                        </button>
                        <button
                          type="button"
                          onclick="toggleEditForm('<%= goal.id %>')"
                          class="bg-gray-500 hover:bg-gray-600 text-white text-xs px-2 py-1 rounded"
                        >
                          Cancel
                        </button>
                      </div>
                    </form>
                  </div>
                </td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
        <% } else { %>
        <div class="text-center py-12">
          <p class="text-gray-500 text-lg mb-4">No savings goals found.</p>
          <p class="text-gray-400">
            Create your first goal using the form above!
          </p>
        </div>
        <% } %>
      </div>
    </div>

    <script>
      function toggleEditForm(goalId) {
        const form = document.getElementById("editForm-" + goalId);
        if (form.classList.contains("hidden")) {
          form.classList.remove("hidden");
        } else {
          form.classList.add("hidden");
        }
      }
    </script>
  </body>
</html>
