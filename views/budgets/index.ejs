<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Budgets - FinTrack</title>
    <link href="/css/output.css" rel="stylesheet" />
  </head>
  <body class="bg-gray-100 min-h-screen">
    <nav class="bg-blue-600 text-white p-4">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-xl font-bold">FinTrack</h1>
        <div class="flex items-center space-x-4">
          <a href="/auth/dashboard" class="hover:text-blue-200">Dashboard</a>
          <a
            href="/auth/logout"
            class="bg-red-500 hover:bg-red-700 px-3 py-1 rounded"
            >Logout</a
          >
        </div>
      </div>
    </nav>

    <div class="container mx-auto mt-8 px-4">
      <div class="max-w-4xl mx-auto">
        <h1 class="text-3xl font-bold text-gray-800 mb-8">Budget Management</h1>

        <div class="bg-white rounded-lg shadow-md p-6 mb-6">
          <h2 class="text-xl font-semibold text-gray-800 mb-4">
            Create New Budget
          </h2>
          <form
            action="/budgets/create"
            method="POST"
            class="flex flex-wrap gap-4"
          >
            <div class="flex-1 min-w-48">
              <label class="block text-gray-700 text-sm font-bold mb-2"
                >Category</label
              >
              <input
                type="text"
                name="category"
                placeholder="e.g., Food, Transportation"
                required
                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
              />
            </div>
            <div class="flex-1 min-w-48">
              <label class="block text-gray-700 text-sm font-bold mb-2"
                >Amount</label
              >
              <input
                type="number"
                step="0.01"
                name="amount"
                placeholder="0.00"
                required
                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
              />
            </div>
            <div class="flex items-end">
              <button
                type="submit"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded"
              >
                Create Budget
              </button>
            </div>
          </form>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6">
          <h2 class="text-xl font-semibold text-gray-800 mb-4">Your Budgets</h2>

          <% if (budgets && budgets.length > 0) { %>
          <div class="overflow-x-auto">
            <table class="min-w-full bg-white">
              <thead class="bg-gray-50">
                <tr>
                  <th
                    class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                  >
                    Category
                  </th>
                  <th
                    class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                  >
                    Amount
                  </th>
                  <th
                    class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                  >
                    Actions
                  </th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-gray-200">
                <% budgets.forEach(budget => { %>
                <tr>
                  <td
                    class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900"
                  >
                    <%= budget.category %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    $<%= budget.amount %>
                  </td>
                  <td
                    class="px-6 py-4 whitespace-nowrap text-sm font-medium space-x-2"
                  >
                    <form
                      action="/budgets/update/<%= budget.id %>"
                      method="POST"
                      class="inline-block"
                    >
                      <input
                        type="text"
                        name="category"
                        value="<%= budget.category %>"
                        required
                        class="px-2 py-1 border border-gray-300 rounded text-xs"
                      />
                      <input
                        type="number"
                        step="0.01"
                        name="amount"
                        value="<%= budget.amount %>"
                        required
                        class="px-2 py-1 border border-gray-300 rounded text-xs w-20"
                      />
                      <button
                        type="submit"
                        class="bg-green-500 hover:bg-green-700 text-white px-3 py-1 rounded text-xs"
                      >
                        Update
                      </button>
                    </form>
                    <form
                      action="/budgets/delete/<%= budget.id %>"
                      method="POST"
                      class="inline-block"
                    >
                      <button
                        type="submit"
                        onclick="return confirm('Are you sure you want to delete this budget?')"
                        class="bg-red-500 hover:bg-red-700 text-white px-3 py-1 rounded text-xs"
                      >
                        Delete
                      </button>
                    </form>
                  </td>
                </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
          <% } else { %>
          <div class="text-center py-8">
            <p class="text-gray-500 text-lg">No budgets created yet.</p>
            <p class="text-gray-400">
              Create your first budget using the form above.
            </p>
          </div>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
