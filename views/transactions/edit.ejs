<!DOCTYPE html>
<html>
<head>
    <title>Edit Transaction</title>
</head>
<body>
    <h1>Edit Transaction</h1>
    <a href="/transactions">← Back</a>
    
    <form method="POST">
        <p>
            Category: <input type="text" name="category" value="<%= transaction.category %>" required>
        </p>
        <p>
            Type: 
            <select name="type" required>
                <option value="">Select</option>
                <option value="income" <%= transaction.type === 'income' ? 'selected' : '' %>>Income</option>
                <option value="expense" <%= transaction.type === 'expense' ? 'selected' : '' %>>Expense</option>
            </select>
        </p>
        <p>
            Amount: <input type="number" name="amount" step="0.01" value="<%= transaction.amount %>" required>
        </p>
        <p>
            Date: <input type="date" name="date" value="<%= new Date(transaction.date).toISOString().split('T')[0] %>" required>
        </p>
        <p>
            <button type="submit">Update</button>
            <a href="/transactions">Cancel</a>
        </p>
    </form>
</body>
</html> 